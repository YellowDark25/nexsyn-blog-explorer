# Redireciona todas as rotas para o index.html
/*    /index.html   200

# Garante que o service worker seja servido corretamente
/service-worker.js /service-worker.js 200

# Garante que o sitemap seja servido corretamente
/sitemap.xml /sitemap.xml 200

# Garante que o robots.txt seja servido corretamente
/robots.txt /robots.txt 200
